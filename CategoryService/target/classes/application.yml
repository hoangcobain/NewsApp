server:
  port:8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CategoryService
    password:
    username:
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: CATEGORY-SERVICE
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
debug: true

